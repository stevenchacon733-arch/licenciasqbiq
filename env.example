# Se llena automáticamente al conectar Vercel Postgres desde el dashboard — no lo edites a mano.
POSTGRES_URL=

# Inventá una clave larga y aleatoria (por ejemplo con `openssl rand -hex 32`).
# Es la contraseña que vas a usar VOS para entrar al panel /admin.html.
ADMIN_SECRET=
